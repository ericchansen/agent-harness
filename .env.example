# Azure AI Services endpoint (required for live mode, not needed with --mock)
AZURE_ENDPOINT=https://your-resource.cognitiveservices.azure.com/

# Optional: override the deployment name from config.json
# AZURE_DEPLOYMENT=gpt-4o
